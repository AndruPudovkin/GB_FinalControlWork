package model;

import java.time.LocalDate;

public class Dankey extends PackAnimak{
    public Dankey(int id, LocalDate age, String namePet, String ownerName) {
        super(id, age, namePet, ownerName);
    }
}
