package model;

import java.time.LocalDate;

public class Camel extends PackAnimak{
    public Camel(int id, LocalDate age, String namePet, String ownerName) {
        super(id, age, namePet, ownerName);
    }
}
